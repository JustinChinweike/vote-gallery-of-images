import{Component,inject,OnInit}from'@angular/core';import{Router,RouterLink,RouterLinkActive,RouterOutlet}from'@angular/router';import{AuthService}from'./auth.service';
@Component({selector:'galleryvote-root',standalone:true,imports:[RouterLink,RouterLinkActive,RouterOutlet],template:`<header><a class="brand" routerLink="/">Gallery<span>Vote</span></a><nav><a routerLink="/" routerLinkActive="active" [routerLinkActiveOptions]="{exact:true}">Explore</a>@if(auth.signedIn){<a routerLink="/contests/new">Create contest</a><span class="user">{{auth.profile()?.username}}</span><button (click)="logout()">Sign out</button>}@else{<a routerLink="/login">Sign in</a><a class="nav-cta" routerLink="/register">Join now</a>}</nav></header><main><router-outlet/></main><footer>Built for photographs worth discovering.</footer>`})export class AppComponent implements OnInit{auth=inject(AuthService);private router=inject(Router);ngOnInit(){this.auth.loadProfile()}logout(){this.auth.logout();this.router.navigateByUrl('/')}}
