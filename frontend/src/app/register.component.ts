import{Component,inject}from'@angular/core';import{FormBuilder,ReactiveFormsModule,Validators}from'@angular/forms';import{Router,RouterLink}from'@angular/router';import{AuthService}from'./auth.service';
@Component({standalone:true,imports:[ReactiveFormsModule,RouterLink],template:`<section class="auth"><p class="eyebrow">JOIN THE COMMUNITY</p><h1>Create your account</h1><form [formGroup]="form" (ngSubmit)="submit()"><label>Username<input formControlName="username" autocomplete="username"><small>3–50 characters</small></label><label>Email<input type="email" formControlName="email" autocomplete="email"></label><label>Password<input type="password" formControlName="password" autocomplete="new-password"><small>At least 10 characters</small></label>@if(error){<p class="error">{{error}}</p>}<button [disabled]="form.invalid||busy">{{busy?'Creating account…':'Create account'}}</button></form><p class="auth-note">Already registered? <a routerLink="/login">Sign in</a></p></section>`})export class RegisterComponent{private fb=inject(FormBuilder);private auth=inject(AuthService);private router=inject(Router);error='';busy=false;form=this.fb.nonNullable.group({username:['',[Validators.required,Validators.minLength(3)]],email:['',[Validators.required,Validators.email]],password:['',[Validators.required,Validators.minLength(10)]]});submit(){this.busy=true;this.error='';const v=this.form.getRawValue();this.auth.register(v.username,v.email,v.password).subscribe({next:()=>this.router.navigateByUrl('/contests/new'),error:e=>{this.error=e.status===409?'That username or email is already registered.':'Could not create your account.';this.busy=false}})}}
